name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '14'

      - name: Install dependencies
        run: |
          npm install -g expo-cli
          npm install
          
      - name: Authenticate with Expo
        run: expo login -u ${{ secrets.EXPO_USERNAME }} -p ${{ secrets.EXPO_PASSWORD }}

      - name: Build Android
        run: expo eas build -p android --profile preview

      - name: Upload artifact
        uses: actions/upload-artifact@v2
        with:
          name: app
          path: .expo/build/android

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v2
        with:
          name: app
          path: .expo/build/android

      - name: Deploy to Android device
        run: |
          # Use a method to send the APK URL to your Android device
          # For example, you can use curl to send an email or push notification
