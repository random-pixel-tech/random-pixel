import React from 'react';
import { Center, Text } from '@gluestack-ui/themed';

const CaptureAttendance = () => (
  <Center flex={1} bg="#EBEAFF">
    <Text>Capture Attendance</Text>
  </Center>
);

export default CaptureAttendance;
