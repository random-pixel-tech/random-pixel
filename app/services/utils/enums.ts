export enum AttendanceStatusLabel {
    All = 'All',
    Present = 'Present',
    Absent = 'Absent',
    OnLeave = 'On Leave',
  }
  
  export enum AttendanceStatus {
    Present = 'present',
    Absent = 'absent',
    OnLeave = 'on-leave',
  }
  
  export enum AttendanceSession {
    Morning = 'morning',
    Afternoon = 'afternoon',
  }